# Vibe Coders 統合計画書

**作成日：2026-09-14（日本時間）｜文書版：1.0｜状態：設計案**

本書は、これまでに決めた要件を一つにまとめた実装計画である。過去の計画書を併読しなくても全体を把握できる構成とする。文書版は製品バージョンではない。以下のコマンド、設定スキーマ、型、ツール名は実装する契約の案であり、実装・公開済み機能を示すものではない。npm名の確保、パッケージ公開、各OSでの動作検証は未実施。

## 1. 作るもの

**Vibe Codersは、リポジトリを拠点にAtom Memoryで記憶を使い、情報を集め、ターミナルやGUIを操作し、必要なら別のエージェントも使う常駐エージェントである。**

人間のバイブコーダーが行う、調べる、コードを読む・直す、CLIに依頼する、アプリを入れる、起動して画面を確認する、後で様子を見る、といった操作を任せる。足りない道具は自分で導入・接続できる。人はチャット中心のWebUIから指示し、必要なときだけ同じ端末やデスクトップへ入る。

人にしか提供できない情報や本人操作が必要なら、小さな入力カードや画面の引き継ぎを出す。**この依頼は独立した非同期機能であり、依頼を出したことを理由に親エージェント全体を止めない。** 他に進められる作業がなく、その入力なしでは本当に進めない場合だけ待機する。

### 基本方針

| 決定 | 意味 |
|---|---|
| 基本YOLO | 日常の編集・実行・導入・設定変更にVibe Coders独自の都度承認を挟まない |
| 必須ファイルは二つ | リポジトリには `atom.toml` と `AGENT.md` だけを要求する |
| 同じ作業場所 | 親、新規shell、Codex、Claude Code等は、指定がなければ同じリポジトリで起動する |
| 汎用の道具を重視 | 専門機能を網羅するより、読取・編集・実行・観測・時間・入力依頼を使いやすくする |
| 行動を固定しない | 計画→実装→レビュー、固定担当、専用フォルダ、worktree、巡回間隔を強制しない |
| 普段の環境を使う | 普段のブラウザへMCPで接続し、人とAIが同じGUIを扱う |
| 本人の一手だけ求める | APIキーの貼付け、ログイン等だけを依頼し、前後の設定・確認は自動で行う |
| 入力依頼は非同期 | フォーム、認証、回答待ちは独立して進み、親は別の仕事を続けられる |

固定の行動規則を隠れたsystem promptに移すこともしない。ハーネスが持つのは道具の使い方、入出力、保存、認証、実行・接続の管理まで。何をどう進めるかは `AGENT.md`、ユーザーの指示、現在の観測から親が判断する。

## 2. 最初の製品範囲

単一所有者が、一つのHomeリポジトリから起動して使う構成を初期対象とする。複数の端末、子エージェント、入力カード、予定は扱える。組織向け権限管理、マルチテナントSaaS、分散クラスタを最初から作らない。

初期の完成範囲には、CLIセットアップ、Webチャット、Atom、ファイル操作、shell・PTY、予定、非同期の人間入力、動的MCP接続、普段のブラウザ利用、Computer Use、WebUIからのGUI操作を含める。GUIと人間入力を「将来の構想だけ」にしない。

LSP、高度なブラウザ診断、形式別専用エディター、巨大なタスクボード、アプリ別の自動化フローは必須にしない。必要なときにCLI、MCP、プラグインで足せるようにする。

「どのCLIも起動できる」と「すべてのCLIの意味や内部状態を正確に取得できる」は区別する。全OS・全GUI・全サービスで無人完結すると約束せず、検証済みの対象と未対応の能力を示す。

## 3. 導入と最初の設定

公開後に目指す操作は次のとおり。

```sh
npm i -g vibe-coders
vibe-coders setup

cd my-agent-repo
vibe-coders init
vibe-coders
```

`setup` は、その端末で使うモデル接続とWebUIのBasic認証を設定する。秘密は伏字入力で受け取り、通常のコマンド引数へ書かせない。初回のモデル接続前には親が推論できないため、この最小の認証だけはCLI自身で完結できるようにする。

`init` は `atom.toml` と `AGENT.md` を作る。既存ファイルは勝手に上書きしない。notes、goals、plans、tasks等は生成必須にしない。

ブラウザ、追加MCP、子CLIをすべて事前に設定させない。最初のモデルと基本ツールが使えれば起動し、足りないものを起動後に親が整えられるようにする。最初のモデルが未認証でも、設定画面と人間入力機構は利用できる構成にする。

親の推論先はOpenAI互換APIまたはCodexサブスクを選べる。CodexサブスクはChatGPTログインで認証し、Vibe Codersの親ループからResponses接続を呼ぶ。ツール・MCP・記憶・予定の所有者は本アプリのまま。親の認証待ちは本人ログインの完了イベントで元の依頼へ戻り、利用枠不足をAPI課金へ自動で切り替えない。

再設定用CLIの例は `auth web`、`provider configure`、`mcp add`、`codex login`、`computer connect`、`config`、`doctor`。人向けの対話形式と、親が呼べる非対話・機械可読形式を同じ操作サービスにつなぐ。ブラウザやOS側に必要な登録・許可は、利用できる操作経路で進め、不可能な一手だけ人へ依頼する。

## 4. リポジトリと設定の分離

### 4.1 Homeとcwd

```text
my-agent-repo/
├─ atom.toml
└─ AGENT.md
```

選択した `atom.toml` のあるディレクトリをHomeとする。親のファイル操作、新規shell・PTY、子エージェントの初期cwdは、明示指定がなければHomeにする。

既存shell内の `cd` はそのshellだけに作用する。他の端末や新規端末の既定cwdを変えない。プロセス全体のcwdを一括変更する実装にしない。OSの `HOME` や子CLIの認証ストアをHomeへ置き換えることもしない。

子用の別フォルダ、worktree、branchは強制しない。必要なら親またはユーザーが通常のshellで作る。同じ場所を複数の子が編集する可能性は表示し、ファイル衝突を観測できるようにするが、固定のGitフローは設けない。

共有するのは作業場所であって、子の会話、モデル入力、認証、記憶まで自動的に共有されるわけではない。

### 4.2 四つの保存範囲

| 保存先 | 内容 |
|---|---|
| `AGENT.md` | 役割、目的、好み、判断方針。自由な自然言語 |
| `atom.toml` | 持ち運べるエージェント構成。モデルの論理名、記憶設定、道具の選択、相対配置、共有ルーチン |
| 端末側の設定・資格情報 | 実際の接続URL、APIキー、Web認証、実行ファイル、ブラウザ・デスクトップの接続、保存先 |
| 内部状態 | 会話、Atom DB、端末、実行、イベント、入力カード、予定の実行位置、ログ、再開情報 |

端末側設定はCLIまたはWebUIから管理する。利用者に三つ目の必須設定ファイルを手書きさせない。内部状態は既定でリポジトリ外のユーザー領域へ保存する。Git cloneだけで認証・DB・生きているプロセスまで複製されるとは扱わない。

`AGENT.md` は必須見出しやタスク形式を要求しない。メモや補助スクリプトを必要に応じて作るのは自由で、Atomだけへ記憶を保存しても成立する。

### 4.3 持ち運べるatom.toml

最小構成はスキーマ版と既定値で成立させる。以下は項目を明示した提案であり、数値や周期は例である。

```toml
version = 1

[agent]
model = "main"

[memory]
engine = "atom-memory"
context_tokens = 6000

# 任意の接続名。実際のURL・認証は端末側に割り当てる。
[mcp]
use = ["browser"]

# 任意。登録しなければこの周期起動は発生しない。
[[routines]]
id = "check-agents"
every = "10m"
action = "prompt"
text = "動いているエージェントの様子を見て、必要なら対応して。"
```

`main` と `browser` は論理名。同じrepoを別PCへ持っていったら、CLIでその端末の実接続に対応付ける。画像入力等の能力が足りなければ不足を示す。ブラウザ等の補助接続が未設定でも、利用可能な道具で親を動かせるようにする。

Basic認証、APIキー、秘密を指す環境変数名、ポート、証明書、shellやDBの絶対パスは共有設定へ入れない。実行IDを含む「この端末を10分後に確認」も内部状態へ置く。

動的に追加したローカル接続は、共有設定の変更なしに当該環境で有効化できる。別環境でも使いたい選択だけを、明示的に `atom.toml` へ反映する。設定とUI更新は同じ保存先を使い、競合を無言で上書きしない。

## 5. 全体構成

```text
                   チャット中心のWebUI / CLI
                              │
                   共通の操作サービスと状態
                              │
          ┌───────────────────┼────────────────────┐
          │                   │                    │
     親のモデルループ     実行・接続の管理     人への入力依頼・予定
          │                   │                    │
       Atom Memory      shell / PTY / MCP       回答・時刻・通知
          │             ブラウザ / GUI             │
          └────────────── イベント受信箱 ────────────┘
```

基本は一つのバックエンドとWebUIで実装する。表中の責務は別マイクロサービスを要求するものではない。画面・入力にOS側の部品が必要なときだけ、対象端末にブリッジを置く。

親の `ModelAdapter` は一回のモデル呼び出しを担当する。子の `NativeAdapter` は複数の推論・ツール実行を含むエージェント作業を担当する。この二つを一つのAPIとして偽装しない。

バックエンドが端末・入力依頼・予定を所有するため、WebUIを閉じても実行は続く。常駐とはバックエンドがイベントを受け取れることを意味し、LLMを無意味に回し続けることではない。

## 6. 親のモデルループとAtom Memory

一回のモデル呼び出しでは、現在のユーザー入力・イベント・観測を取り込み、`AGENT.md`、必要な実行状態、直近の文脈から入力を作る。その都度Atomの記憶を取得し、その呼び出しだけの記憶領域へ入れる。

前回自動注入した記憶を、次の自動検索へ無条件に戻さない。最初のユーザー質問だけを永遠に検索条件にするのでもなく、現在のツール結果や明示的な焦点を反映する。

取得したAtomの参照・版とモデル応答を記録する。利用ack、binding、出典、整合性、古い整理の扱いは採用するAtom Memory版の契約に従う。記憶を取得しただけの場合と、モデル入力へ供給して成功応答を得た場合を混同しない。[S1]

親は通常の記憶ツールから保存・修正・整理できる。専任Writer、定期整理、特殊な親Atom型、用途別の新しい記憶モデルを必須にしない。実行中プロセスや入力カードの状態は、自然言語の記憶検索ではなくランタイムの実データで扱う。

ツール呼び出しと結果、モデル継続に必要な項目は保持する。非公開CoTを読み出す設計ではない。モデル呼び出し境界で記憶・設定・使用可能ツールを更新し、送信済みの推論リクエストを途中で書き換えない。

モデルが次の行動を選ぶ際は、直接操作、子への委譲、人への入力依頼、予定登録、通常応答、待機を選べる。人への依頼が未回答でも、ほかの選択肢は失われない。

## 7. 基本道具

少数の汎用能力を使いやすく実装する。以下の名前は提案。専用の道具があることと、毎回その道具を使うことを強制することは別である。

| 道具 | 最初に提供する能力 |
|---|---|
| ファイル・検索 | 一覧、範囲読取、glob・文字列・正規表現検索、作成、限定置換、差分編集 |
| shell | 明示cwdでのコマンド実行、stdout/stderr、終了コード、長時間実行、停止 |
| ターミナル | 継続PTY、出力差分、現在画面、文章・キー入力、サイズ変更、手動操作への引き継ぎ |
| Web | 公開情報の検索、URL取得、本文・出典・取得範囲を返す |
| ブラウザMCP | 普段のブラウザのタブ、本文・表示内容、基本操作、必要な画像 |
| Computer Use | 同じデスクトップの画面取得、クリック、文字入力、キー、スクロール、ドラッグ |
| 記憶 | Atomの自動供給、明示検索、参照、保存、修正 |
| 実行・接続 | 実行状態とログ、MCPの追加・接続・再読込、任意の子CLI・ネイティブ接続 |
| 時間・イベント | 単発・周期予定、実行イベントの購読、後で起きる条件、取り消し |
| 人への依頼・対話 | 非同期の入力カード、本人操作への引き継ぎ、回答・通知 |

基本のファイル操作をすべてshellへ丸投げしない。一方、Git、install、build、test、deploy等は既存CLIでも扱えるので、製品固有の必須ワークフローへ変換しない。

### 道具共通の品質

長い出力は範囲、続きの取得位置、省略の有無を返す。編集は現在内容と照合し、部分適用や衝突を全成功と表示しない。画像はパス文字列だけでなく実際のモデル入力へ渡す。未対応・未設定は明示する。

即時に終わる処理は結果を返す。長い処理、外部認証、人待ちが発生し得る処理は管理IDを返し、バックエンドで実行を持つ。モデルから見た元のtool callには受理結果を一度だけ返し、後の完了は別イベントにする。完了するまで親のツール処理を無期限に開き続けない。

## 8. ターミナルと子エージェント

### 8.1 任意CLIの入口

PTYで普通のshellや指定プログラムを起動し、管理用セッションIDをすぐ返す。親は出力を読み、指示を送り、別の作業へ戻れる。ユーザーもWebUIから同じ端末を開いて操作できる。

`terminal.open/read/screen/write/key/resize/list/close` 程度の共通操作を用意する。単発の `shell.exec` は、複数回の実行でshell内の状態が維持されると扱わない。継続状態が必要なら同じPTYを使う。

TUIには、生出力の追記ログだけでなく、制御シーケンスを反映した現在画面も返す。日本語、幅広文字、代替画面、サイズ変更、Ctrl+C、貼付けを検証する。通常の端末読取にOCRを要求しない。

プロセスが生きていること、入力待ち、モデルのターン終了、仕事の成功は別。汎用CLIで分からない状態は `unknown` と返す。

### 8.2 任意のネイティブ接続

CodexやClaude Codeへの公開API・構造化出力による接続はプラグインにする。Codex App Serverはスレッド・ターンや実行イベントを扱う接続候補。Claude Codeにはプログラム実行と構造化出力の経路がある。[S2][S3]

起動、読取、追加指示、中断、再開、cwd、取得できる状態等の実能力を宣言する。即時の途中入力と次ターンへの予約は区別する。プラグインがなくてもPTYの経路は使える。

PTYがないネイティブ接続には偽の端末を作らず、会話・イベントと実際に可能な操作を表示する。接続が壊れたら同じ仕事を勝手にPTYで再投入せず、既存実行を照合する。

`AGENT.md` は親の指示ファイルとして扱い、子に必要な内容は対応する入力経路で渡す。子が同名ファイルを自動読込すると決めつけず、別の必須ファイルを人に管理させない。既存の子向け指示ファイルも勝手に上書きしない。

### 8.3 子への記憶

まずは依頼・追加入力へ選んだ情報を渡し、必要ならAtom検索をMCP等で公開する。子の全内部モデル呼び出しへの記憶交換は、対応プラグインやproxyによる任意拡張とする。これを本体の必須依存にしない。

モデルの通常のAPI接続と子CLIの認証は別に管理する。proxyやプラグインを付ければ利用権や上限まで変わるとは扱わず、採用先の条件と認証方式を実装時に確認する。

## 9. ブラウザとComputer Use

### 9.1 情報を取るためのWeb・ブラウザ

Web検索・取得は、推論に必要な公開情報を取る道具。ブラウザMCPは、ログイン済みページや実際の表示を読むための道具。常にブラウザを開く、常にGUIから操作する、といった順番は固定しない。

ブラウザは普段使うプロファイルとタブへ拡張型MCPで接続する。Chrome等が未導入なら親が通常のブラウザを導入してよいが、既存環境を無断で置き換えない。Playwright、Puppeteer、WebDriverで別の自動化用プロファイルを立ち上げる経路を標準にせず、接続失敗時も自動で切り替えない。

既存の拡張型MCPが要件を満たせばそれを使う。自前ブリッジが必要な場合、Chrome拡張とローカルアプリをつなぐNative Messagingは候補になる。これはブラウザ側ホスト登録を伴う仕組みであり、MCPの通信形式そのものとは別である。[S4]

初期に必要なのはタブの選択・移動、本文・表示内容の取得、基本操作、必要時の画像取得。高度なコンソール・通信解析や独自ブラウザ製品の開発は必須にしない。MCPツールの名前とschemaは実接続から取得する。

普段のブラウザを使っても、自動化検出やサービス側の制約を回避できる保証にはしない。認証Cookieを抽出して別プロファイルへ複製することを前提にしない。

### 9.2 人とAIが同じGUIを操作する

```text
AIの画面取得・入力 ──────┐
                        ├─ 同じデスクトップ／GUIアプリ
人のWebUIデスクトップ ──┘
```

Computer Useは、画面をモデルへ渡し、モデルが選んだクリック・キー等をホストで実行する道具とする。モデル接続だけで操作先OSが自動的に用意されるとは扱わない。

ユーザーにはWebUIでライブ画面とマウス・キーボード操作を提供する。アプリを個別のWeb画面に作り替えず、実際のGUIを表示する。AIには必要な時点のスクリーンショットを渡し、ライブ映像の全フレームをモデルへ送り続けない。

実装候補は、既存デスクトップを共有するサーバー／ブリッジと、WebUIに埋め込むnoVNC。noVNCはブラウザ側のVNCクライアントで、接続先のサーバーとWebSocket経路を別途必要とする。[S5]

AIの観測・入力もバックエンドから同じ対象へつなぐ。ユーザーがデスクトップのWebUIを開いていることを、AIが動く条件にしない。初版で独自の映像配信基盤や全OS向け入力実装をすべて開発しない。

画像対応モデル、画面サイズ、DPI、ディスプレイと座標系を確認する。接続断、画面切替、手動操作後は古い座標を再利用せず再観測する。ロック画面、保護されたOS操作、権限不足は未対応・操作不能として報告する。

### 9.3 操作場所と引き継ぎ

ブラウザ・PTY・デスクトップがどのPCにあるかを表示する。Vibe CodersがWSLやサーバーにある場合も、手元のWindows等を接続できる構造にする。ローカルPC、WSL、別ホストのパスやファイルが自動共有されるとは扱わない。

人が端末やデスクトップを引き継いだら、対象へのAIの未送信入力を失効させる。ブラウザMCPによる同じ対象への変更も調停する。返却後は最新状態を観測して続ける。

**引き継ぎは対象リソースの操作権だけを切り替える。親全体を止めない。** 人がデスクトップで操作している間、親は別のshell、ファイル、記憶、子の監視を使える。GUIのキーボードフォーカスを共有する対象は、必要に応じてデスクトップ単位で調停する。

## 10. 足りない環境を自分で整える

Chrome、CLI、MCPサーバー、拡張、補助スクリプトが不足していたら、親は既存のshell、Web取得、Computer Use、設定CLIで導入できる。アプリ別インストーラーや環境構築専任エージェントをコアに持たない。

### ChromeとMCPを使い始める例

対象OS・ホスト・既存環境を確認し、提供元の手順を調べ、必要なブラウザ・拡張・MCPを導入する。実接続をローカル設定へ登録し、接続、ツール一覧の取得、実際のタブ読取まで確認して元の仕事へ戻る。

これは一つの利用例であって固定フローではない。すでに使える環境があれば導入し直さない。アプリの導入成功、プロセスの起動、MCPの接続、対象ページの取得は別々の観測として扱う。

対象へ一切の操作経路がない状態で、会話だけから部品を配置できるとは扱わない。最初のモデル、ファイル、shell、ブラウザに依存しないURL取得、設定管理、MCPクライアントを土台にする。GUI操作が必要になる時点では、対象デスクトップへの接続が必要。

### MCPの動的追加

接続管理は、人が使うCLI・WebUIと親で共有する。親は非秘密の設定を登録し、接続の開始・再読込・状態確認を行える。MCPを追加するために、別の未設定MCPを必要とする循環は作らない。

MCP接続では、初期化による版・能力の交渉を行い、`tools/list` 等から実際の能力を取得する。対応先のツール一覧変更通知にも追従する。[S6][S7]

取得した道具は次のモデル呼び出しから利用可能にする。設定保存だけで接続済みとは表示しない。新規MCPのために親の会話を再起動することは通常要件にしない。ただし、稼働中コードを読み込むプラグインの更新まで無再起動を保証するものではない。

接続の追加・削除時も、進行中のツール呼び出しと結果の対応を維持する。実行先の能力不足、認証不足、ブラウザ未接続を明示する。導入元、版、対象ホスト、変更内容を記録し、再試行前は残っている状態を確認する。

## 11. 人への依頼は独立した非同期機能にする

### 11.1 最重要の動作契約

**入力要求の登録と、人の回答を待つことを、別の操作にする。**

`human.request` はカードを永続化してIDをすぐ返す。それでモデルから見たtool callは完了する。人が入力するまでPromise、モデルストリーム、会話の実行権を保持しない。

人への依頼を処理するサービスは、フォーム表示、入力受付、秘密保存、関連付け、結果イベントの配信を独立して扱う。専用の「人待ち担当LLM」をもう一体動かす必要はない。

| 状況 | 親の動き |
|---|---|
| 検索APIキーがないが、repoを読める | カードを出し、repo調査を続ける |
| ログインを人に渡しているが、別端末でテストできる | 対象画面だけ人に渡し、別端末を使う |
| 子が認証待ちだが、別の子や自分の作業が進められる | その実行だけ待たせ、他を進める |
| 入力がないと残る作業を進められない | 理由と起動条件を登録して親の推論を待機させる |
| 親モデル自体の認証が失効した | 推論は止まり得るが、フォーム・UI・認証更新は動く |

コアが「入力依頼が一件でもあるなら待機」と決めない。親は利用者の目的に沿った残りの作業や代替手段を判断する。一方、続けられる仕事がないのに活動を捏造し、無意味な推論やポーリングを続けることも要求しない。

### 11.2 APIキー入力の体験

接続の導入や非秘密の設定は親が行い、本人のキーだけが不足した時点でチャットにカードを出す。

```text
検索サービスのAPIキーを入力してください

準備済み：導入・接続設定
使用先：登録済みの検索接続
保存先：実行ホストの資格情報ストア

APIキー  [ •••••••••••••••••• ]
[取得ページを開く]  [保存]  [あとで]  [中止]

この入力とは別に、エージェントは作業を続けられます。
```

カードはチャット入力欄や全画面を塞ぐ必須モーダルにしない。独立した「入力依頼」一覧にも同じカードを表示し、チャットが流れても後から対応できるようにする。

送信後は、ホストが秘密を保存・関連付け、準備された接続で確認できる範囲を検証して結果イベントを届ける。追加の「貼った」「続けて」は不要。人の入力だけで設定スクリプト全体を最初から再実行することはしない。

取得ページや必要な権限は親が調べて提示する。既存の認証で足りる場合は再入力を求めない。人には設定ファイルの編集やコマンドのコピーまで丸投げしない。

### 11.3 最小の操作契約

親に渡す道具の案は `human.request`、`human.status`、`human.cancel`。通常は完了イベントを使い、statusの繰り返し呼び出しで待たない。

```ts
// 提案するデータ契約。実装済みAPIではない。
type HumanRequestSpec = {
  kind: "input" | "secret" | "action";
  title: string;
  message: string;
  targetId?: string;     // 登録済み接続や画面。秘密入力では必須
  fields?: InputField[]; // ホストが検証する限定フォーム定義
  resumeNote?: string;  // この入力が何に必要だったか。実行コードではない
  dedupeKey?: string;
};

type InputField = {
  name: string;
  label: string;
  type: "text" | "choice" | "secret";
  required?: boolean;
  options?: { label: string; value: string }[];
};

type HumanRequestReceipt = {
  requestId: string;
  state: "pending" | "processing" | "resolved" | "cancelled" | "expired";
};
```

`kind: "input"` は非秘密の短いテキストや選択肢、`secret` はホスト自身の専用秘密入力、`action` は本人ログインや画面への引き継ぎ。通常入力に秘密が必要なら、秘密経路へ切り替える。

型だけで用途を許可するのではなく、ホストが種類とfields、接続、保存先を検証する。`input` に `secret` フィールドを混ぜない。モデルや外部MCPから任意のHTML・JavaScriptを渡して認証画面として実行させない。

```text
親 → human.request(...) → { requestId: "hr_42", state: "pending" }
親 → ファイルを読む／子へ指示する／別の設定を進める
人 → カードへ入力
ホスト → 保存・必要な関連付け・状態確認
ホスト → human.resolvedイベントを受信箱へ追加
親 → 次の安全な処理境界で、現在の状態と一緒に結果を読む
```

新規要求の受理状態は `pending`。同じ要求の再送では既存IDと実際の状態を返し、完了済みを未回答へ戻さない。

ここでの `human.resolved` は、回答の受理、取消、期限切れ等の結果を届けるVibe Coders内部のイベント名案である。接続成功を常に意味するものではない。結果にはrequest ID、結果の種別、非秘密の回答、または参照IDと `saved` / `ready` / `invalid` / `unverified` 等の確認結果を区別して載せる。入力修正が必要なら同じカードの新しい版で受け付け、既に適用した操作まで繰り返さない。

### 11.4 回答を受けたら、古い会話へ巻き戻さない

要求には発行元、所有者、会話ID、関連実行ID、対象接続の版、目的、発行イベントを結び付ける。発行時のメモは「何に必要だったか」を理解するための情報であり、当時のモデル状態へ巻き戻す命令ではない。

親が別の作業を進めている間に回答が届いたら、同じ受信箱へ保存する。次のモデル呼び出し・ツール処理の安全な境界で取り込む。回答の到来だけで実行中の正常な推論や子を中断しない。長い外部処理は管理IDへ分離し、そもそも親の入力受付を占有させない。

親は現在の履歴、作業の進み具合、回答を合わせて判断する。既に別の方法で終えていたら終了を維持する。新しい指示で不要になったら取消・保留する。入力を受けたからといって過去の公開・送信・作成を再実行しない。

フォームの受理、保存、接続確認はモデルが一時的に使えないときも進められる。結果は保存し、モデルが復帰したときに通知する。ユーザーが明示的に親を一時停止した場合、回答だけでその停止を解除しない。

### 11.5 本当に進めない場合だけ待機する

待機は、入力要求の副作用ではなく、親が選ぶ独立した制御操作とする。例として `agent.wait` を用意する。

```json
{
  "reason": "必要なデータの取得には hr_42 の認証が必要で、今進められる準備は完了している",
  "wakeOn": [
    { "type": "human.resolved", "requestId": "hr_42" },
    { "type": "user.message" }
  ]
}
```

待機は条件を永続化したうえで現在の親の推論実行を終える。daemon、フォーム、タイマー、動いている子、ユーザーのチャット受付は止めない。通知条件を保存する直前に回答が届いていた場合も、既存の結果を読み直し、通知の取り逃しで永遠に眠らないようにする。

依存関係の完全なTask DAGを必須にせず、実行ID・要求ID・待ち条件で必要な範囲を表せばよい。代替作業の有無や重要度は親が判断する。入力が何件か溜まっただけで、全件が揃うまで一律に停止しない。

### 11.6 秘密は、モデルへ教えるのでなく接続に預ける

秘密は普通のチャット送信経路へ流さない。専用入力から資格情報管理へ送り、登録された接続・用途へ束縛する。モデルへ返すのは参照と状態だけで、原文のキーではない。

キーは通常のtool引数・結果、会話DB、Atom、`atom.toml`、repo、標準ログに含めない。参照IDだけで誰でも秘密を取り出せる仕組みにしない。HTTPヘッダー、特定プロセスの環境、専用IPC等への供給は実行直前にホストが行い、すべてのshellへ無条件に配らない。[S9]

Basic認証のパスワードは検証用ハッシュ、利用時に必要なAPIキーは資格情報ストア等で別管理する。認証ページに直接入力する一時コードは、通常の永続記憶へ保存しない。

同一OSユーザーで任意shellを使えるYOLO環境なので、悪意あるプラグインや侵害された実行コードからの完全な秘密隔離は保証しない。目標は通常経路でモデル・記録に漏らさず、宛先と用途を限定すること。正規の接続先や対象プロセスに渡した秘密が、その先で安全に扱われるかは別途検証する。

### 11.7 本人操作中は対象の観測だけを止める

パスワード、MFA、パスキー等の入力時は、その対象へのAIの入力だけでなく、画面、DOM・フォーム値、ネットワーク本文、クリップボード等の管理下の観測も止める。秘密がデスクトップ全体に映る場合は、そのデスクトップ全体のAI観測を一時停止する。子やMCPの同じ対象への管理下アクセスも対象にする。

**停止するのはその観測・入力経路であり、親の推論と他の仕事ではない。** 人がログイン中でも、親は別の情報源・端末で作業できる。WebUI上の秘密フォームとエージェントが読む画面を同一視せず、管理画面自身がAIの画面取得に映る場合も対処する。

返却は、安全な画面または非秘密の認証状態を確認してから行う。完了ボタンは本人の申告であり、接続成功とは分ける。管理できない外部の観測経路がある場合は「AIから見えない」と保証せず、別の本人用認証経路を使う。

### 11.8 外部MCPや子の人待ちも、親から切り離す

外部MCPの呼び出し内で追加質問が来た場合、その接続・実行を管理下の非同期実行として保持し、人へのカードを出す。外部RPCの応答待ちが続く場合でも、親から見える呼び出しには管理IDを返し、親の会話実行権を占有させない。MCPへ架空の `pending` 応答を返してプロトコルを変更するのではなく、ホスト側の実行管理で分離する。

外部のタイムアウト・キャンセル・再開能力には従う。接続が切れたRPCを再起動後にそのまま生きていると扱わない。能力に応じて再認証・再確認を行い、副作用のある元の処理を無条件に再送しない。

MCPの2025-11-25仕様では、通常のform elicitationでAPIキー等を要求せず、秘密を扱う第三者認証にはURL modeを使う。Vibe Coders自身に預ける専用秘密入力と、外部サーバーの認証ページは別経路にする。URL方式は宛先を表示して本人が開き、URLを開いたことを認証完了と見なさない。[S8]

URL認証の完了通知が来れば利用し、来なければ再確認・中止を残す。認証ページをAIが自動で取得・観測する入口にはしない。外部仕様が求める同意をYOLOで省略しない。[S8]

汎用PTYの任意の入力欄を、必ず秘密フォームへ自動変換できるとは約束しない。対応アダプターがない場合は端末・GUIを必要な範囲だけ人へ渡す。そのCLIが待っても親は動ける。

## 12. 時間・イベント・能動性

能動性は、ユーザー入力、選択した実行のイベント、予定時刻、接続や人の回答等を、同じ受信箱へ届けることで作る。どの通知で何をするかは親やユーザーが決める。

予定には、親へ指示を届ける動作と、コマンド等の登録済み操作を実行する動作を用意する。単発、周期、対象実行のイベントを扱う。UIと親は同じ予定を作成・編集・無効化・削除・今すぐ実行できる。

「5分ごとに必ずレビュー」「60分で必ず目標整理」をコアに固定しない。親が「10分後に確認する」と判断したなら、実際に予定を登録する。文章に書いただけでタイマーが生まれることはない。

持ち運ぶルーチンは `atom.toml`、実行ID付きや端末限定の予定は内部状態に置く。日次等にはタイムゾーンを明示する。最終実行位置と配信履歴は設定から分離する。ホスト停止中に溜まった周期を一回にまとめる等の配信方針は見える設定にし、無制限な追いつき実行を既定にしない。

子を見るときはまず状態・出力差分を取得できるようにし、毎回「進捗どう？」と入力することを必須にしない。無出力だけで異常や失敗と断定しない。トークン断片や端末再描画のたびに親を起こさない。

入力カードの有無と周期実行は独立させる。入力待ちのカードが残っていても予定やユーザー入力は受け付ける。同じ不足を理由に毎回新しい入力カードを作らず、既存要求を再利用する。明示的に中止した要求を予定だけで復活させない。

実行可能な仕事も受信イベントもなければ待機する。周期的にモデルを起こす設定には費用が発生し得ることを表示する。必要な動作には、バックエンドと対象PCが稼働していることが前提となる。

## 13. WebUI

チャットを基本画面とし、必要なときだけ作業画面を開く。独自IDEや承認ワークフロー製品にしない。

| 領域 | 内容 |
|---|---|
| チャット | 指示、回答、画像・ファイル添付、ツールカード、追加入力、親の一時停止・停止 |
| 入力依頼 | 未回答のテキスト・秘密・本人操作カード。後回し、送信、中止、状態確認 |
| ターミナル／セッション | shell・子CLI・ネイティブ実行、現在画面、出力差分、追加入力、手動引き継ぎ |
| デスクトップ | 実際のGUIのライブ表示、クリック・キー、表示調整、AIとの操作切替 |
| 予定 | 単発・周期・イベント、保存範囲、次回時刻、編集、停止、今すぐ実行、履歴 |
| 補助表示 | ファイル、差分、成果物、Atom、設定、接続状態、診断 |

カードから対象の端末・接続・予定へ直接移動できるようにする。UIとエージェントは同じ操作サービスを使い、人だけに見える裏設定や別のタスク状態を作らない。

**親の状態と、入力カード・子・画面の状態は別に表示する。** 例えば「親：実行中／入力依頼：2件／Codex：稼働中／デスクトップ：人が操作中」が同時に成立する。カードがあるだけで画面全体を「入力待ち」にしない。

「親の推論を一時停止」「この子を停止」「デスクトップ操作を引き継ぐ」「全停止」を区別する。全停止は予定等からの新規実行も止め、管理下実行に停止を要求する。要求済みと実際に停止したことを分けて表示する。

ブラウザの再読込・タブ終了とバックエンド停止は別操作。再接続すると同じ会話・カード・端末・予定に戻る。ただし、操作対象のブラウザ自体を閉じればブラウザMCPは使えなくなるので、その状態は表示する。

## 14. YOLO、認証、秘密の境界

通常の作業を許可する方針はYOLOのままにする。編集、インストール、子の起動、設定変更、予定登録、公開やデプロイ等のために、製品独自の固定レビュー・都度承認を増やさない。実行ユーザー、接続先、利用契約、OSが与えていない権限は取得済みと扱わない。

入力の形式検証、認証、出力の範囲管理、操作記録、停止機構、競合制御は行動ワークフローではなく、道具を正しく動かすための機構として維持する。モデルステップ、費用、並列数の業務上の固定上限は設けず、必要なら明示的な任意設定とする。通信タイムアウトやバッファ上限等の実装上必要な境界は別に設定する。

### Webの入口

WebUIは単一所有者のBasic認証。認証設定はCLIから端末側に保存する。検証用パスワードハッシュを使い、APIキーの保存とは分離する。既定listenはloopback。外部アクセスにはHTTPSを使う。Basic認証だけでは通信を暗号化しない。[S11]

HTTP API、添付・ログ、秘密入力、ターミナルとデスクトップのWebSocketをすべて保護する。状態変更にCSRF対策、WebSocketに接続時認証とOrigin検証を行う。生のパスワードをURLへ埋め込まない。必要なら認証済みHTTPから短命・一回限りの接続ticketを渡す。[S10]

MCP・ブラウザ・デスクトップのブリッジは個別に認証し、WebのBasicパスワードを流用しない。上流のVNC等を無認証で公開しない。管理画面と生成アプリのプレビューは別オリジンにし、管理用資格情報を転送しない。

### 観測と実行の限界

Web本文、repo内の文章、子の出力、MCPの説明は、取得した資料・観測として区別する。上位命令や認証設定へ自動昇格させない。表示するMarkdown・端末出力・フォーム定義を安全に処理し、任意HTMLから管理APIを触れる構成にしない。

同じOSユーザーで任意のコードを実行できることには、ファイル削除、認証情報、ネットワーク操作等への影響がある。YOLO、Git worktree、ログの伏字を強い隔離と宣伝しない。必要な隔離はユーザー・コンテナ・ホストの配置で選べるようにする。

## 15. 保存・並行処理・復帰

### 15.1 最小の内部データ

| データ | 保存する情報 |
|---|---|
| 会話 | 入力、モデル応答、継続に必要な項目、処理済みイベント位置 |
| 実行・セッション | run/session ID、種類、ホスト、cwd、外部ID、生存確認材料、状態、ログ位置 |
| イベント | ID、発生元、所有者、対象、原因、時刻、非秘密のpayload、処理位置 |
| 人への依頼 | request ID、種類、表示定義、対象の版、状態、非秘密の結果参照、関連実行 |
| 予定・待ち条件 | 時刻・周期・イベント条件、保存範囲、次回実行、通知済み位置 |
| 接続の投影 | ローカル設定の版、能力、状態、資格情報への参照 |
| Atom | 採用ライブラリの永続ストアと索引 |

秘密の原文は会話・要求DBへ入れず、資格情報管理へ分離する。Task、Project、Goal、Review、Approvalの専用業務エンティティを必須にしない。並行処理に必要なrun ID等は、仕事の進め方を固定するものではない。

### 15.2 親の判断は整列し、周辺機能は止めない

同じ会話のモデル履歴を二つの親判断で同時更新しない。ユーザー入力、回答、予定、子のイベントは永続受信箱へ取り込み、処理境界で順に渡す。

フォームの受付、秘密の保存、接続確認、子プロセス、画面配信、タイマーは、その親判断のロックを持って待たない。秘密の入力中も、同じデスクトップを使わない別の作業を続けられるようにする。

応答待ちの外部処理には独立した実行ハンドルを持たせる。特にMCP内の人待ちを、単一の `await tool()` に埋め込んで親を塞がない。能力を持つ外部接続ではその非同期機構を使い、そうでない接続はホストで保留実行を管理する。

### 15.3 二重実行と古い結果

要求登録・入力受付は操作IDと版で照合する。同じカードの二重送信、タイマーの再送、同じ外部完了通知で設定や次作業を二重実行しない。取り消し、期限切れ、対象接続の変更後に届いた古い回答は、その古い目的へ適用しない。

要求の結果とイベント配送を同じDBトランザクションまたは永続outboxで結び付け、保存できた回答の通知を失いにくくする。イベント処理は重複排除を前提にする。

秘密ストアへの保存とDB更新、DBと外部プロセスは単一の原子的操作ではない。途中失敗時はoperation IDと対象の版から照合し、秘密の孤立保存や適用漏れを回収する。外部に跨る完全なexactly-onceを保証しない。

### 15.4 再起動・一時停止・取消

WebUI再接続では既存状態を表示する。バックエンドやOSが停止してプロセスが消えた場合、記録から「稼働中」と偽らず、中断・不明を返す。PIDだけで同じプロセスと判定しない。子CLIの会話resumeと、PTYプロセスの生存は別である。

入力カードと受信済み回答は復元するが、過去のモデルチェックポイントへ無条件に戻さない。現在の会話と実在の接続・成果物を確認して続ける。クリック、送信、デプロイ、起動コマンドの不明な結果は観測してから判断する。

カードの取消はその依頼に作用し、他の作業や動いている子を巻き込まない。親の一時停止中にもカード入力は受け付けて保存できるが、ユーザーが停止した親を勝手に動かさない。全停止後の新規作業開始には再有効化が必要。

ログ、出力、スクリーンショットには保持方針を設ける。画面や秘密を無条件にAtomへ蓄積しない。復旧に必要な状態と巨大な生ログの保存量を分ける。

## 16. 実装の分け方と順番

実装にはユーザー指定の **TypeScript / Bun / Hono / React** を採用する。内部状態のDBは **SQLite / Drizzle ORM** とし、一つのコードベースとWebUIで構成する。モデルループ、操作サービス、イベント、保存、UIを分離するが、配布パッケージやサーバーを最初から大量に分割しない。Atom Memoryは独立ライブラリのまま利用する。実装済み範囲と検証・未完了項目は [docs/implementation.md](docs/implementation.md) を参照する。

| 段階 | 実装すること | 次へ進むための確認 |
|---|---|---|
| A：起動と親 | CLI、二つのrepoファイル、ローカル設定、Basic認証、Webチャット、Atom、ファイル・shell | 秘密をrepoに置かず起動し、親自身が読み・編集・実行できる |
| B：非同期基盤 | イベント受信箱、長時間実行、PTY、人への依頼・専用秘密入力、待機と復帰 | キー入力を待つ間も親が別ツールを使い、入力後は現在の会話で続ける |
| C：接続と時間 | 設定CLIの非対話化、動的MCP、予定UI、同じrepoでの任意CLI、ブラウザMCP | 新しいMCPを次の推論で使え、UIを閉じても予定・子・入力カードが生存する |
| D：共有GUI | 一つのデスクトップ接続、人のWeb操作、同じ画面のAI操作、対象単位の引き継ぎ | 普段のGUIを人とAIが共有し、認証中も別の仕事は動く |
| E：統合検証 | 環境の自己構築、キー入力だけの補助、復旧・競合・秘密保護、配布診断 | 以下の統合シナリオと受け入れテストを通す |

ネイティブ子接続は対応先から追加し、PTY経路を残す。子内部proxy、LSP、高度な文書・ブラウザ機能、追加OSの拡充は、基本体験を通してから増やす。

A〜Eを今回の初期完成範囲とする。人への依頼を最後にボルトオンせず、イベント基盤と一緒に実装する。全ツールを同期呼び出しとして完成させてから非同期化する順番は避ける。

## 17. 統合シナリオ

### 17.1 普通の作業と任意の子

repoで起動し、WebUIから変更を依頼する。親がAtomとファイルを読み、自分で編集するか、同じrepoのPTYでCodex等へ依頼する。子が動く間に親は別の確認を行い、必要な時点で出力を読んで追加指示する。ユーザーも同じ端末へ入れる。

### 17.2 Chrome未導入からMCPを使う

基本ツールと対象GUIの接続だけがある検証環境で、ChromeとブラウザMCPは未導入にする。親が対象を確認して導入元と手順を調べ、通常ブラウザと必要な接続部品を導入・登録する。会話を再起動せずに新しい道具を取得し、対象ページを読む。OS等で本人操作が必要な場合は、その箇所だけ人へ依頼する。

### 17.3 APIキーを求めながら、親は作業を続ける

検索接続のキーがない状態で、repo調査と検索接続の準備を依頼する。

```text
時点1  親：接続準備を進め、APIキー用カード hr_42 を登録
時点2  親：登録結果を受け取り、repoのコードと設定を読む
時点3  親：子へ別の確認を依頼し、実行結果を収集
時点4  人：hr_42 へキーを貼り付けて送信
時点5  ホスト：保存・接続確認を行い、結果を受信箱へ追加
時点6  親：現在の作業状況に結果を加え、使える検索を必要なら利用
```

この試験では、人が回答する前に親が別のtool callを実行したことをログで確認する。フォームを出して同じtool callが回答まで開いたままなら不合格。送信後に元の導入操作を最初から再実行した場合も不合格。

### 17.4 本当に進めないときだけ待つ

唯一のデータ取得に認証が必要で、他の準備が終わっている状態を作る。親は必要なカードと待ち条件を登録し、推論を終了する。待機中に不要なモデル呼び出しを増やさず、回答またはユーザーの別指示で現在状態から動く。daemonや子、入力受付を停止してはいけない。

### 17.5 本人ログイン中も別の仕事を続ける

人へ認証画面を渡し、その画面のAI観測と入力を止める。同時に、親が別端末でテストやファイル読取を続ける。認証後、安全な状態を再観測して操作を返す。同じ画面を共有しつつ、全面的なエージェント停止を必要としないことを確認する。

## 18. 受け入れテスト

### 非同期の入力依頼を最優先で検証する

| ID | 条件 | 合格基準 |
|---|---|---|
| H01 | 入力カードを発行 | request IDが返り、回答前に親が別の道具を使える |
| H02 | 二枚以上の未回答カード | カードの存在だけで親、子、予定、チャット受付が止まらない |
| H03 | 実行中に回答が届く | 次の安全な境界で取り込み、同じ会話の親判断を二重起動しない |
| H04 | 発行後に別作業が進んだ | 古い履歴・計画へ巻き戻さず、現在状態に回答を加える |
| H05 | 入力なしでは本当に進めない | 明示待機に入り、無意味な推論を回さず回答で起きる |
| H06 | 待ち条件登録と回答が競合 | 先に保存された回答も取り込み、起床を失わない |
| H07 | 二重送信、取消後通知、対象設定の変更 | 対象と版を照合し、二重適用・誤接続・不要な再開をしない |
| H08 | カード表示後にUIを閉じる | カードと他の実行が生存し、再接続で同じ要求へ回答できる |
| H09 | 親モデルが利用不能 | UIと秘密保存・接続確認は動き、結果が復帰後に届く |
| H10 | ユーザーが親を一時停止 | 入力は保存できるが、回答だけで停止を解除しない |
| H11 | 無効なキーと一時的な通信障害 | 状態を区別し、通信不良で何度もキーを再要求しない |
| H12 | 外部MCPまたは子が人待ち | その実行は保留でき、親の別tool callが実行できる |
| H13 | 秘密・本人認証の区間 | 対象の管理下観測は止まるが、別端末・ファイルの作業は続く |
| H14 | 一意なテスト用秘密を入力 | モデル入力、会話DB、Atom、repo、通常ログに原文が出ない |

### 全体の整合性

| ID | 条件 | 合格基準 |
|---|---|---|
| C01 | 新規導入・別PCへの持ち運び | 二つのrepoファイルだけで構成し、実接続・秘密はローカル設定で対応できる |
| C02 | 親、shell、任意CLI・子を起動 | 指定なしなら同じHome。ある端末のcdが他へ波及しない |
| C03 | TUI、日本語、入力引き継ぎ | 現在画面とログを読め、手動移譲後に古いAI入力が混入しない |
| C04 | プラグインがないCLI | PTYから使える。未取得のターン完了や成功を偽らない |
| C05 | WebUIタブを閉じる | 管理下端末、予定、接続、入力要求がUIの生存に依存しない |
| C06 | 新規MCPを登録 | 初期化・ツール取得・実動作確認後に利用でき、親の再起動を要求しない |
| C07 | ブラウザ接続が失敗 | 別の自動化ブラウザへ無断で切り替えず、原因を返す |
| C08 | 人とAIがGUIを操作 | 同じデスクトップを扱い、DPI・日本語・ドラッグ・再観測を確認できる |
| C09 | WSLとWindows等が混在 | 導入先、画面、ファイル、接続ホストを混同しない |
| C10 | Atomの自動記憶 | 毎回の観測を使い、前回注入分の自己反復を避け、採用版のack契約を守る |
| C11 | 周期予定と一時予定 | 保存範囲が分かれ、再起動や重複通知で無制限に再実行しない |
| C12 | ツール実行直後のクラッシュ | 実在状態を照合し、不明な副作用をそのまま再送しない |
| C13 | プロセスが失われた | 記録だけで稼働中と表示せず、子のresumeとプロセス復元を区別する |
| C14 | 未認証アクセス、CSRF、不正Origin | API、秘密入力、端末・デスクトップへ到達できない |
| C15 | 外部MCPの秘密要求 | 通常formへAPIキーを流さず、適切な認証経路と状態確認を使う |
| C16 | AGENT.mdだけを変更 | 専用Task登録、レビュー工程、固定フォルダを要求せず行動を変えられる |
| C17 | 全停止 | 新規起動を抑止し、管理下実行の停止要求と停止確認を表示できる |

## 19. 実装時に検証して決める項目

| 項目 | 判断の基準 |
|---|---|
| Node・PTY・GUI部品の対応OSと配布 | 最初の検証環境で導入可能な組み合わせを固定し、追加OSは実測で拡大する |
| 既存デスクトップの共有方式 | 普段のGUIを人とAIが実際に共有できるか。空の別セッションだけで要件達成としない |
| 拡張型ブラウザMCP | 普段のブラウザ、必要な読取と基本操作、引き継ぎが機能するか |
| モデル・Atom Memoryの採用版 | ツールと画像の能力、継続方式、記憶契約、費用を固定ケースで確かめる |
| Codex／Claude Codeのネイティブ接続 | 対応API、認証、途中入力、再開、外部側の人待ちを検証する |
| 保存・イベント実装 | 入力要求、回答、秘密保存、待機、再起動の競合を再現テストできるか |

これらは選定・検証項目であり、利用者へ毎回判断を戻すための未定事項ではない。実装者はまず一つの組み合わせで全体を動かし、契約とテストを保って広げる。

## 20. 参考資料と位置付け

製品の要件は、この会話でのユーザー指定を統合したもの。外部の技術的な入口は次の一次資料を参照した。ブラウザ・GUI等の候補を採用済み・実機検証済みとする意味ではない。

S2〜S10は2026-09-14にページを確認。MCPは明示した2025-11-25仕様を参照しており、最新版と断定しない。S1は会話内で取得したAtom MemoryのREADME／runtime仕様、S11は本統合時に確認した認証資料を指す。APIの詳細と利用条件は採用版に固定して実装する。

- **[S1] Atom Memory：保存・検索とホスト側のエージェント実行の責務。** `https://github.com/tako0614/atom-memory/blob/main/README.md` ／ `https://github.com/tako0614/atom-memory/blob/main/docs/runtime.md`
- **[S2] Codex App Server：ネイティブ接続の候補。** `https://developers.openai.com/codex/app-server/`（参照時の転送先：`https://learn.chatgpt.com/docs/app-server`）
- **[S3] Claude Code programmatic execution：CLIの構造化実行。** `https://code.claude.com/docs/en/headless`
- **[S4] Chrome Native Messaging：拡張とローカルアプリの接続。** `https://developer.chrome.com/docs/extensions/develop/concepts/native-messaging`
- **[S5] noVNC Embedding：ブラウザ側のデスクトップ表示・操作の候補。** `https://novnc.com/noVNC/docs/EMBEDDING.html`
- **[S6] MCP Lifecycle（2025-11-25）：初期化・能力交渉。** `https://modelcontextprotocol.io/specification/2025-11-25/basic/lifecycle`
- **[S7] MCP Tools（2025-11-25）：一覧取得と変更通知。** `https://modelcontextprotocol.io/specification/2025-11-25/server/tools`
- **[S8] MCP Elicitation（2025-11-25）：通常フォームとURL認証の区別。** `https://modelcontextprotocol.io/specification/2025-11-25/client/elicitation`
- **[S9] OWASP Secrets Management：秘密の保存・配信・ログ露出の設計。** `https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html`
- **[S10] OWASP WebSocket Security：接続認証とOrigin等の入口の保護。** `https://cheatsheetseries.owasp.org/cheatsheets/WebSocket_Security_Cheat_Sheet.html`
- **[S11] MDN HTTP Authentication：Basic認証とHTTPS。** `https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Authentication`

---

**最終的な設計原則：道具、記憶、実行環境、時計を渡す。必要な環境は自分で整える。人には本人の一手だけを非同期で依頼する。その返答待ちだけでエージェント全体を止めず、本当に進めないときだけ待機する。**
